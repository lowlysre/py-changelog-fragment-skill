name: Validate skill schema

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  agnix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: agent-sh/agnix@d18c0a88e6a61878502f105106d054bcce9789df # v0.52.1
        with:
          path: .
          strict: 'true'
